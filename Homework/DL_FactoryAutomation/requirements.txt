# DL_FactoryAutomation — 딥러닝 기반 설비 고장 예측
# ML 프로젝트 의존성 + TensorFlow 추가

# ── ML 공통 ───────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
streamlit>=1.35

# ── DL 전용 ───────────────────────────────────────────────────
# 저장된 모델이 Keras 3 형식(.keras, keras_version 3.12)이므로
# Keras 3를 로드할 수 있는 TF 2.16+ 스택이 필요하다.
# (구버전 tensorflow-cpu==2.15.0 = Keras 2.15 는 .keras(Keras3) 로드 불가 → 앱이 "미학습" 오표시)
tensorflow-cpu>=2.16      # Keras 3 번들 (CPU 전용)
keras>=3.0                # .keras(Keras 3) 모델 로드 보장
# torch>=2.1              # PyTorch 사용 시 주석 해제

# ── 시각화 ────────────────────────────────────────────────────
matplotlib>=3.7
seaborn>=0.13
plotly>=5.18

# ── 코드품질 ──────────────────────────────────────────────────
ruff>=0.4
pytest>=8.0
# ── MLflow 실험 추적 (로컬 SQLite) ────────────────────────────────
mlflow>=2.12
sqlalchemy>=2.0
